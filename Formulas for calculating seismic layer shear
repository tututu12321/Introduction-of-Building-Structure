# 定数の定義
Z = 0.9  # 地域係数 (例)
R = 1.0  # 振動特性係数 (例)
X = 1.0  # 構造の種類の係数 (例)
A_i = 1.0  # 地震層せん断力係数の分布 (例)
C_0 = 0.2  # 標準せん断力係数 (例)
W = 10000  # 建物の重量 [N] (例)

# 地震層せん断力係数 C の計算
C = Z * R * X * A_i * C_0

# 地震層せん断力 Q の計算
Q = C * W

# 結果の表示
print(f"地震層せん断力 Q: {Q} N")
